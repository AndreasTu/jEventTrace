rootProject.name = "jEventTrace"


file('subprojects').eachDir { dir ->
    include dir.name
    project(":${dir.name}").projectDir = dir
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}
